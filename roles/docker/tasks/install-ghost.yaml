---
- name: "configure ghost"
  become_user: parker
  copy:
    src: ../apps/ghost/
    dest: ~/ghost/

- name: "run ghost"
  become_user: parker
  docker_compose:
    project_src: ~/ghost/
    state: present
